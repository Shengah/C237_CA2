<!DOCTYPE html>
<html>
 <head> 
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

then in body right under body add this 
<%- include('style') %> <!--Keep this at the very top just after </body> -->

then just before u close </body> add this line 
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script> <!--Keep this at the very end just before </body> -->
<body>
    <h1>Sleep Logs</h1>
    <table>
        <thead>
            <tr>
                <th>Log ID</th>
                <th>User ID</th>
                <th>Sleep Date</th>
                <th>Sleep Time</th>
                <th>Wake Time</th>
                <th>Sleep Quality</th>
                <th>Mood After</th>
                <th>Notes</th>
            </tr>
        </thead>
        <tbody>
            <% if (sleepLogs.length > 0) { %>
                <% sleepLogs.forEach(log => { %>
                    <tr>
                        <td><%= log.logId %></td>
                        <td><%= log.userId %></td>
                        <td><%= log.sleepDate.toISOString().split('T')[0] %></td>
                        <td><%= log.sleepTime %></td>
                        <td><%= log.wakeTime %></td>
                        <td><%= log.sleepQuality %></td>
                        <td><%= log.moodAfter %></td>
                        <td><%= log.notes %></td>
                    </tr>
                <% }) %>
            <% } else { %>
                <tr>
                    <td colspan="8">No sleep logs found.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>
